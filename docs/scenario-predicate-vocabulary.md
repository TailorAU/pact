# Scenario predicate vocabulary

<!-- AUTO-GENERATED by sites/source/scripts/generate_predicate_vocabulary.py -->
<!-- Do not hand-edit. Re-run the generator after seeding / deprecating scenarios. -->

This document lists every predicate key in use across `scenarios.predicates` and `scenario_applies_when.predicate`, together with observed values and the scenarios that depend on them. Scenario matching is case- and spelling-sensitive, so synonyms silently fragment the graph. See handoff #1160 §11.6 for discipline.

**New keys:** if you need a new predicate key, either reuse the canonical spelling already listed below, or add an entry to `DEPRECATED_KEYS` in the generator with the old → new mapping in the same PR.

Total keys in use: **0**
Total scenarios: **0** (0 live, 0 deprecated)

> **Bootstrap placeholder.** This file is regenerated from the database by
> `sites/source/scripts/generate_predicate_vocabulary.py`. The first run
> against a populated dev/prod DB will replace this placeholder with the
> live vocabulary. CI's `scenario-pr-check` workflow treats a stale file
> as a failure once seed data has landed.

## Predicate keys

| Key | Values / pattern | # scenarios | Seen on |
|-----|------------------|-------------|---------|
| _(regenerate against a populated DB)_ | — | — | — |

## Per-key detail

_(regenerate against a populated DB)_

## Deprecated keys (retired synonyms)

_None yet. When a predicate key is retired in favour of a canonical spelling, add it to `DEPRECATED_KEYS` in `generate_predicate_vocabulary.py` so the CI gate can surface the mapping._
