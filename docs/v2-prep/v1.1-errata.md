# PACT v1.1 — errata

> **Status:** Draft errata. Once approved, this file moves to `spec/v1.1/ERRATA.md`.
> Per [AGENTS.md](../../AGENTS.md), `spec/v1.1/SPECIFICATION.md` itself stays frozen for citation stability — the errata note documents known issues without amending the frozen text.

## E1 — Phantom section references in preamble

**Location:** [spec/v1.1/SPECIFICATION.md](../../spec/v1.1/SPECIFICATION.md), lines 14–16.

The "What's New in v1.1" preamble references:

- *"Resource Types (Section 15)"*
- *"Implementation Profiles (Section 16)"*
- *"Conformance Levels (Section 16)"*

These sections do not exist in `spec/v1.1/SPECIFICATION.md`, which ends at §14 (Open Questions). The intended content lives in the v2.0 draft as §14 and §15.

**Reader guidance.** For Resource Types, Implementation Profiles, and Conformance Levels, see:

- [spec/v2.0/SPECIFICATION.md §14](../../spec/v2.0/SPECIFICATION.md) — Resource Types
- [spec/v2.0/SPECIFICATION.md §15](../../spec/v2.0/SPECIFICATION.md) — Implementation Profiles and Conformance

(`spec/v2.0/` was created on 2026-05-13 by collapsing the canonical v1.2-draft per decision D1.)

**Upstream fix:** the §14/§15 cross-references are correct in `spec/v2.0/SPECIFICATION.md`. The fix was not back-ported into the frozen `spec/v1.1/SPECIFICATION.md` per AGENTS.md rule 4 — this errata note is the canonical disclosure for v1.1 readers.

## E2 — Schema `$id` paths point to `/v0.4/`

**Location:** All files in [spec/v1.1/schemas/](../../spec/v1.1/schemas/).

Every schema in `spec/v1.1/schemas/` carries a `$id` of the form:

```
https://pact-spec.dev/schemas/v0.4/{name}.json
```

These should reference `v1.1`, not `v0.4`. The `$id` was carried forward unchanged across the v0.4 → v1.0 → v1.1 promotions.

**Reader guidance.** Treat the `$id` as a stable URI for the schema content. The path component does not reflect the spec version the schema is associated with. The schemas in `spec/v1.1/schemas/` ARE the v1.1 schemas regardless of `$id` path.

**Upstream fix:** `spec/v2.0/schemas/*.json` use `$id` paths matching their version directory (`https://pact-spec.dev/schemas/v2.0/...`). The v1.1 schemas remain as-is for citation stability per AGENTS.md rule 4.

---

## Filing record

Public closure record: [TailorAU/pact#15](https://github.com/TailorAU/pact/issues/15). This errata note is the canonical disclosure; it promotes to `spec/v1.1/ERRATA.md` once Knox green-lights writing inside the frozen `spec/v1.1/` directory (a new, additive file — does not amend `SPECIFICATION.md`).

Last updated: 2026-05-13.
