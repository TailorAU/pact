# Generated by sites/source/scripts/scenario_golden_check.py.
# Weekly CI run (scenario-golden.yml) and ad-hoc local runs drop JSON reports
# here for historical diffing; we don't commit them.
scenario-golden-*.json

# Keep the directory itself.
!.gitignore
