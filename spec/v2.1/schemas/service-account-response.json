{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pact-spec.dev/schemas/v2.1/service-account-response.json",
  "title": "PACT Service Account",
  "description": "Response for POST /api/pact/service-accounts and POST /api/pact/service-accounts/{id}/rotate. `credential` appears ONLY in create and rotate responses and MUST NOT be retrievable afterwards; list responses omit it. Added in PACT v2.1 (Section 22.3).",
  "type": "object",
  "required": [
    "service_account_id",
    "name",
    "owner_principal_id",
    "scope",
    "created_at"
  ],
  "properties": {
    "service_account_id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "owner_principal_id": {
      "type": "string"
    },
    "scope": {
      "type": "object",
      "properties": {
        "resources": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Resource / fabric ids the account may act on. Empty or absent means NO resource access — never 'all'."
        },
        "sections": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Section ids within those resources. Absent = all sections of the named resources."
        },
        "operations": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "read",
              "propose",
              "constrain",
              "vote",
              "close",
              "subscribe",
              "mint-mandate"
            ]
          },
          "description": "Operations permitted for this account."
        },
        "mandates": {
          "type": "object",
          "properties": {
            "may_mint": {
              "type": "boolean",
              "description": "Permits minting Section 20 Mandates. The Mandate's signature is STILL the HumanPrincipal's (20.5 step 4) — a service account never signs a Mandate on a human's behalf."
            },
            "max_disclosure_ceiling": {
              "type": "integer",
              "minimum": 1,
              "maximum": 4,
              "description": "Caps the disclosure_ceiling of any Mandate minted through this account, independent of what the handler requests."
            }
          },
          "additionalProperties": false,
          "description": "The Section 20 seam."
        }
      },
      "additionalProperties": false,
      "description": "Scope dimensions. An update MAY narrow a scope; it MUST NOT widen one (widening requires a new account, so an audit trail always shows the grant) — an attempt MUST be rejected with scope.widening_forbidden."
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "expires_at": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    },
    "credential": {
      "type": "string",
      "description": "The bearer credential. Returned ONCE, on create and rotate only. MUST NOT be retrievable afterwards."
    },
    "previous_credential_valid_until": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time",
      "description": "Rotation overlap window (SHOULD be at least 60 seconds, MUST be documented in the Implementation Profile) so a running daemon picks up the new credential without a failed request. Rotation MUST NOT widen scope."
    },
    "revoked_at": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    }
  },
  "additionalProperties": false
}
